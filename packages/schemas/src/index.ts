/**
 * Zod schemas shared between apps/api (request/response validation) and
 * apps/web (react-hook-form resolvers) — a single source of truth for
 * "what's a valid item name" etc. (docs/06-frontend-architecture.md §1).
 *
 * Empty during scaffolding; no business schemas exist yet.
 */
export {};
